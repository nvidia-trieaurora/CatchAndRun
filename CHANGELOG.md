# Changelog

Tracker các version của Catch & Run — mỗi version ghi rõ đã làm gì.

## [Unreleased]

No changes yet.

## [2.0.0] — 2026-09-17 · Harbor V2

Release scope: all merged Harbor V2 work through 2026-09-17.

### Added
- Harbor RP03 native Market and metric island-surface pass, moving boat support for Hunters/Props/dropped objects, precise low-skiff dry contact on client/server, shore-contact foam and filtered ripples. Versioned Blender sources, asset backups and verification notes: `docs/v2/harbor/island-review/README.md`.
- Harbor RL01 across six production GLBs: non-destructive Blender sources, contact shading in vertex colours, 20 supported bolt plates in three batches, preserved gameplay colliders/rigs, colour-only compression, six native 4K review renders and a separately labelled concept board. Reproduction and rollback records: `docs/v2/harbor/realism-review/README.md`.
- Add temporary ground debris effects and keep wall bullet marks until the next round within each room.
- Add the Harbor V2 stylized-realism art bible, production asset pipeline, and Warehouse vertical slice.
- Expand Harbor V2 into a detailed seawall island surrounded by animated water, with tree-leaf and water-splash bullet impacts plus lethal water for Hunters and Props.
- Add the Harbor cinematic migration: modular GLB/PBR zone assets, renderer and ocean backends with WebGL fallback, private Water Pro integration seam, golden-hour environment lighting, quality presets, and performance budgets.
- Port MIT-licensed interactive-water techniques from `jeantimex/threejs-water` into the TSL ocean backend, including impact ripples, Fresnel/refraction tuning, and high-tier caustics without adopting its WebGL-only pool renderer.
- Rebuild Harbor water with directional Gerstner waves, visible crest and shoreline foam, proximity warnings, a drowning camera sequence, and an expanded authored harbor fleet and environment-detail kit.
- Establish Warehouse and Working Dock as the Harbor V2 fidelity benchmark with CC0 PBR materials, a weathered roof/facade pass, complete stair rails, market and seawall dressing, detailed animated boats, mobile LOD batching, and repeatable development snapshot views.
- Warehouse PBR01 material pass authored through Blender MCP: Poly Haven CC0 Base/Normal/ORM sets for concrete, red box-profile facade, galvanized steel, navy steel, safety yellow, dark roof and wood; world-space box UVs at real tile sizes; WebP texture export (`EXT_texture_webp`); alpha-blend skylights; per-quality-tier anisotropic filtering and AO intensity in `MapAssetLoader`. Warehouse GLB stays at 4.9 MB with all 86 movement colliders and 7 markers unchanged.
- Harbor V2 edge-zone pipeline for the two waterfront corners, authored in Blender 5.2 through MCP from deterministic builders (`tools/harbor-v2/zones/`): zone `.blend` sources under `art-source/harbor-v2/<zone>/`, `harbor:v2:export-*` / `validate-*` / `preflight-*` scripts, a zone contract validator (naming, budgets, footprint, route-clearance volumes, 488-collider parity against the procedural gameplay dump), staging-only candidates (`_staging/*-candidate.glb`, never auto-promoted), runtime `ZoneOverride`s that remove the legacy cinematic zone meshes and carve merged map-wide batches triangle-wise so old and new visuals never overlap, and a TSL `ZoneAmbientMotion` pass (canopy/shrub/grass/reed sway, lily bob, pond ripple, lamp flicker, crane hook sway) driven by `ambientMotion` extras with seeded per-vertex phase.
- AC — Waterfront Residential Garden candidate (`garden-ac-candidate.glb`, 2.2 MB, 21 LOD0 / 11 LOD1 batches): two-storey plaster house with dark seamed metal roof, ridge cap, gutters, downspouts and chimney, wooden porch (decking, joists, posts, braces, steps, foundation), octagonal koi pond with lily pads and rock island, four connected planter benches, continuous paths, warm interior emissive, porch lamps and card-canopy trees tagged `weaponImpactKind=foliage` / pond `water`; visual-only, built on the existing gameplay colliders.
- AD — Waterfront Construction Site candidate (`construction-ad-candidate.glb`, 2.2 MB, 19 LOD0 / 11 LOD1 batches): three-storey concrete/steel frame on the nine cinematic columns with footings, rebar, red I-beams with gusset plates and bolts, shoring, partial pours and formwork; scaffold tower, bridge and secondary scaffold with stairs and toe boards; silo on plinth and legs with hopper, ladder cage and grating service platform; lattice tower crane with jib, trolley, hoist cables and swaying hook; floodlight mast, brick site gate, open-sided material shelter and collider-matched props. Every moving hit surface carries `ambientMotion`; no collider follows the crane animation.
- Dev-only snapshot presets `acGarden*` / `adConstruction*` (`window.__catchAndRunView`) and the `?harborZones=staging|off` query switch backed by a Vite dev middleware for `art-source/harbor-v2/_staging`; production controls are unchanged.
- Promote both edge zones to production: `client/public/assets/maps/harbor-v2/zones/garden-ac.glb` and `construction-ad.glb` (byte-identical to the validated candidates, metrics alongside), gated by `HarborZoneAsset.promoted` so unpromoted candidates are never requested by production clients. Validation record: `docs/v2/harbor/edge-zones-ac-ad-validation.md`.
- Ferris Harbor District (`art-source/harbor-v2/_staging/ferris-harbor-candidate.glb`, 2.7 MB, validated then promoted on request to `client/public/assets/maps/harbor-v2/zones/ferris-harbor.glb`, `promoted: true`, `?v=20260907-fh01`; validation record `docs/v2/harbor/ferris-harbor-district-validation.md`), authored in Blender 5.2 through MCP by `tools/harbor-v2/zones/build_ferris_harbor.py` on the existing Ferris footprint (hub -10/12/34, mount radius 8.5, eight cabins): industrial coastal wheel with twin lattice rims, spokes, cross bracing, rust-red axle and bearing housings, A-frame legs on concrete footings, hub maintenance platform with drive motor/gearbox/brake/guard, cable conduit, perimeter bulb string, cabin hangers and pivots, five muted cabin colour variants of one modular gondola (glass, roof light, bench, grab rail); working pier with timber boardwalk to x 51, kerb bollard-and-rope chain, concrete promenade with joints/drains/puddles, seawall cap and facing, piles, tyre fenders, cleats, bollards, safety ladders, life rings, harbor lamps, market tables/umbrellas/crates/nets, cable reel, kiosk and ticket booth; unified moored fleet — hero workboat (navy hull, cream wheelhouse, boot-top and antifouling bands, mast, radar, nav lights), red and green skiffs, fishing launch with davit, cargo barge with crates and drums — plus lit buoys. Everything animated is rigged as named nodes: `RIG_FERRIS_HARBOR_WHEEL_ROOT` / `MOUNT_<i>` / `CABIN_<i>` (counter-rotating so gondolas hang upright, driven from the same angle as the unchanged 40 procedural cabin colliders and platform carry), `RIG_FERRIS_HARBOR_BOAT_*` roots with `ambientMotion`, and `SOCKET_FERRIS_HARBOR_MOOR_*` pairs for the mooring lines.
- BD — Eastern Container Yard (`art-source/harbor-v2/_staging/container-bd-candidate.glb`, 1.9 MB, validated then promoted on request to `client/public/assets/maps/harbor-v2/zones/container-bd.glb`, `promoted: true`, `?v=20260908-bd01`; validation record `docs/v2/harbor/container-bd-validation.md`), authored in Blender 5.2 through MCP by `tools/harbor-v2/zones/build_container_bd.py`: the scattered yard re-laid as a working harbour yard — 6 m north-south forklift lane with edge lines, centre dashes, stop line, drains and a zebra to the shop; the legacy east-west road kept as the cross road with a hatched turning bay; two 3 m alleys that keep all four yard prop spawns in the open; two-high 40 ft container blocks along the west and south (doors to the lane, one door ajar with cargo, a dented "damaged" unit), a single-height waterfront row with a reefer, a flat-rack with strapped drums, a maintenance workshop container; a painted loading zone with a chassis trailer carrying a 20 ft box, an idle forklift, safety barriers, cones, cargo net; an empty-container handler; the Mini Mart rebuilt as a metal-clad harbour shop (weathered concrete base, big steel-framed windows with a lit interior, recessed open glass entrance, canopy with a restrained cyan strip, sign, flat roof with parapet, gutter and downspouts, AC condensers, conduit, vending machine and utility cabinet, fire cabinet, service door, bins, roof service stair) with a 6.5 m clear forecourt (raised sidewalk at the slab level, kerb, drains, bollards, bench, bike, hand cart); the yard office as a site cabin with a lit window; south fence with an open gate; weeds, puddles, patches. Modular container kit with corner posts, rails, castings, locking bars, cams, handles, hinges, seals, vents, forklift pockets and targeted rust; all colour variants share one vertex-tinted sheet-metal material (COLOR_0 + grime gradient) so LOD0 is 14 draws / 22.9k tris, LOD1 6 / 346, 14.5 MiB textures. Re-authored collision: the zone ships 47 `COL_MOVE_CONTAINER_BD_*` boxes (exact container envelopes, machinery, sidewalk, bollards, fence, props) and the override drops the 14 legacy `COL_MOVE_CINE_CONTAINER_*` + `COL_MOVE_CINE_FORKLIFT`; Mini Mart, office, lamps, tire stack, seawalls and spawns untouched (runtime colliders 703 → 735). Contract validator: footprint, budgets, no procedural overlap, clearance volumes for lane / cross road / alleys / entrance / doorway / walkway / spawns. Dev presets `containerBD*` (8).
- HARBOR MARKET — the BD shop redesigned as a realistic harbour mini-supermarket (candidate `art-source/harbor-v2/_staging/container-bd-candidate.glb`, 3.9 MB, validated then promoted on request to `client/public/assets/maps/harbor-v2/zones/container-bd.glb`, `?v=20260908-bd02`), authored in Blender 5.2 through MCP by `tools/harbor-v2/zones/harbor_market.py` (called from `build_container_bd.py`) on the shop's gameplay boxes: charcoal vertical metal cladding on a warm concrete plinth, low membrane roof with parapet, flashing, gutter and downspouts, the roof slab continuing 2 m over the entrance as a canopy whose deep fascia carries a real text-geometry sign "HARBOR MARKET" (DIN Alternate Bold → mesh, warm-white emissive on a navy panel, no texture text), broad glazed storefront with a centred 3 m automatic bi-parting door (leaves parked open, operator header, cyan LED canopy strip), east service side (recessed service door with lamp, stacked twin condenser and electrical cabinet on the legacy utility boxes, conduit, meter box, crate stack, pallet, hand truck), forecourt with curved kerb corners, drainage grates, four edge bollards, slatted bench, bike rack. Interior on the top floorplan: entrance mat and anti-theft pedestals, baskets, produce display along the west wall (tiered timber/steel, 17 crates of fruit heaps), two double-sided gondolas with endcaps (aisles 1.6–3.4 m, door axis on the centre aisle), six reach-in refrigerated cases along the rear and right walls (glass doors, handles, lit interiors, product rows), checkout with belt, scanner, register, monitor, terminal, bagging shelf, impulse rack and cashier wall shelf, stockroom + manager room in the rear-left with open doors, window, shelving, desk, chair, fire extinguisher and conduit, suspended ceiling with a 3×4 LED panel grid. 917 low-poly products (cans, jars, tapered bottles, cereal boxes, cartons, bags, row cards) on one procedural 512 px label atlas (`MAT_PRODUCT_ATLAS`, vertex-tinted for variation) — the shop adds two draw calls (atlas + clear glass): zone LOD0 16 draws / 46.2k tris (shop 24.2k, 8 materials), LOD1 7 / 630. Dev presets `harborMarket{Exterior,Front,Left,Interior,Checkout,Top,Shelf,Fridge}`; `HarborMarket.test.ts` pins the floorplan (left side flat, 3 m doorway, one box per fixture, every aisle ≥ 1.5 m, rooms enterable, zone colliders outside the approach/aisles, shop budget ≤ 35k tris / 15 materials).
- AB — HARBOR OPERATIONS & REPAIR LANE (candidate `art-source/harbor-v2/_staging/operations-ab-candidate.glb`, 2.6 MB, validated then promoted on request to `client/public/assets/maps/harbor-v2/zones/operations-ab.glb`, zone `operations-ab` with `promoted: true`, `?v=20260909-ab01`; validation record `docs/v2/harbor/operations-ab-validation.md`), authored in Blender 5.2 through MCP by `tools/harbor-v2/zones/build_operations_ab.py` (`npm run harbor:v2:build-operations-ab`) on the strip between the AD fence and the BD yard: the legacy east-west road as a 6 m inspection lane (edge lines, centre dashes, three inspection rectangles with wheel stops on the kerb-side edge strip, recessed weighbridge with a flush steel deck and control cabinet, drains on both edges, kerbs with roller-door gaps, bollards, kerb-free hatched turning spaces at both ends, two cross-lane escape routes beside the building); the Dockside Cafe Bar re-dressed on its own gameplay boxes as the enterable Operations building — navy vertical cladding on a warm concrete base, flat roof with parapet, gutter and downspout, condensers, spinning roof vent and wall fan, conduit + cable tray, ocean-facing control windows with lit control-room recesses, glazed personnel portal, roller-shutter bay with the curtain rolled up, side door, warm lamps, restrained cyan status strip, antenna mast with beacon, "HARBOR OPERATIONS" as real text geometry on a dark backing across the parapet, no attached stairs — with a playable control office (control desk with monitors and CCTV quads, radios, chart plotter, plan chest with a text-free chart, racks, UPS, lockers, notice board, emergency phone) and workshop (workbench with vise, parts counter with colour-coded bins, shelving, pegboard, compressor, welding cart, hose reel, spare propeller and pump on pallets, tool chest, gas bottles, floor drain) separated by a partial glazed partition with a parked sliding door, circulation loop 1.9–3 m; front apron machines dressed on the legacy deck boxes with a utility cart bay; Warehouse service wall (facade only): three closed roller doors under corrugated canopies on cantilever brackets with downlights, utility risers and a 6.2 m pipe run, cable tray with hangers and swaying cables, downpipes, fire hose cabinet, electrical panels, tool lockers, transformer enclosure with guard posts and chain, maintenance workbench, spare-parts pallets, wall lamps; seawall service pockets: pump house (door, lamp, louvred vent, stack, cyan status), pipe manifold with red valves and gauges, compressor skid, rescue cabinet, life rings and coiled hoses on the rail, seawall lights, drain outfall, small davit on the cap (foundation, rotating post, braced boom, pulley, winch, swaying cable + hook over the water), buoy and tyre racks, maintenance crates, utility cart, painted pedestrian paths, and a checkpoint booth with a raised barrier arm and flashing beacon at the BD end. Zone LOD0 15 batches + 2 rotors / 33.7k tris / 17 draws, LOD1 7 / 344, 12.8 MiB textures, 28 `COL_MOVE_OPERATIONS_AB_*` colliders (large solids only); the override drops the six `COL_MOVE_CINE_BAR_*` colliders that sealed the bar's openings and carves the bar shell / roof overhang / deck furniture out of the shared dock batches plus the legacy lane dashes and puddles (asphalt, seawall rail and the Mini Mart untouched; seawall collider, kill zone and water hazards unchanged, runtime colliders 734 → 756). `ZoneAmbientMotion` gained `spin` (CPU rotor rotation about `spinAxis` at `spinRpm`, meshes and collapsed InstancedMesh batches), `status-pulse`, `beacon-flash` (emissive envelopes on cloned materials so a pulse never rides the shared lamp palette) and `davit-sway` (TSL pendulum, pivot 4.85 m); `MapAssetLoader.collapseRepeatedMeshes` keeps the source `userData` on the InstancedMesh. Dev presets `operationsAB{,Route,Interior,Seawall,Top,Detail,Pump}`; `OperationsABZone.test.ts` (registry not promoted, override scope, 21 clearance volumes vs the 493 procedural colliders, 5 m lane, doorways + interior loop, seawall/kill-zone parity, collider budget/placement), `ZoneAmbientMotion.test.ts`, `WeaponSystem` ambientMotion dynamic-raycast case; `npm run harbor:v2:{build,export,validate,validate-…-gltf,test,preflight}-operations-ab`.
- `ZoneOverride` carving now applies every zone's `carveNames` to a shared merged batch (previously only the first zone naming a batch carved it, so low-tier Ferris carves after AD were skipped); `zone_kit` gained `pbr(vertex_tint=True)` + `TintedMaterial` (per-object COLOR_0 tint, grime gradient, roof dirt).
- Runtime adapters for authored dynamic zones: `FerrisHarborRig` (drives the GLB wheel/cabins from `buildFerrisWheel`'s pivot while `ferrisVisuals: false` strips only the procedural meshes), `MooringRopes` (fixed-size sagging tubes rewritten in place each frame between boat and cleat sockets, straight 2-segment on low tier, never raycast), water-sampled `HarborAmbientMotion` (heave from the Gerstner surface, pitch/roll from its slope under the hull, damped when moored, heave-only buoys, seeded phase so no two hulls bob in step, wakes only for craft flagged as moving — the fleet is moored so none are pooled), `ZoneOverride.removeNamePrefixes` for instanced legacy pier dressing, exporter/validator support for `RIG_`/`SOCKET_` empties and `requiredRigNodes`, dev presets `ferrisHarbor`, `ferrisHarborRoute`, `ferrisHarborWater`, `ferrisHarborTop`, `ferrisHubDetail`, `ferrisCabinDetail`, `workboatDetail`, `fleetDetail`.

### Fixed
- Harbor runtime: preserve variant geometry and transforms during instancing/batching, release unused asset resources safely, respect authored shadow exclusions, cache movement broadphase while tracking moving cabins/decoys, prevent ceiling/crouch tunnelling, retain world-size bullet marks under nonuniform parent scales, and prewarm pooled weapon effects. Ocean normals and CPU/GPU ripple envelopes agree; inactive ripples and unnecessary transmission passes are skipped. Move the Warehouse sign ahead of the fascia on supported brackets.
- Restore both exterior Warehouse stair flights and roof landings in Low LOD: 42 treads and 2 landings exactly match their movement colliders, with batched supports and rails (+1,800 triangles / 2 draws). Pin all 132 sampled walkable surfaces against the shipped GLB after low-tier preparation; preserve High geometry and existing collision data.
- Fix strict ESLint errors blocking GitHub Actions CI.
- Align bullet impacts with rendered surfaces and prevent holes when shots miss geometry, including sky shots.
- Redesign inverted Warehouse roofs; remove overlapping V1 Warehouse geometry; harden vertical and horizontal collision; attach bullet marks to moving Ferris-wheel geometry; make grenade timing and Hunter boosts server-authoritative; replace black blast artifacts; cap aiming movement at 30%; and add deep regression validation.
- Prevent the Harbor scene, minimap, and gameplay controls from leaking into the room lobby during waiting, countdown, results, leave-room, and solo-to-multiplayer transitions.
- Restore visible Warehouse roof-access stairs, align cinematic walls and door openings with movement colliders, correct the Hunter cage to use its east-side dynamic gate across every round, and prevent both invisible doorway blockers and walk-through walls.
- Keep Hunter and Props grounded on the Warehouse stair treads during ascent, descent, and jump landings while smoothing only their visual/camera height instead of the authoritative physics position.
- Make vertical ladders climbable: every stack of rung colliders (silo, secondary scaffold, cargo-net frame, watchtower) is detected as a `LadderVolume` and Hunters/Props grab it by walking into it, climb with W/S at 2.6 m/s with rung-synchronised camera/mesh sway, mantle over the top rung onto the deck, and let go with a hop on jump (`client/src/game/controllers/LadderClimb.ts`). The old "walk up the rungs" step colliders could never be climbed because the body box is blocked by the rungs before the narrower ground probe reaches them.
- Make the construction tower crane climbable: the lattice mast is hollow (leg + wall colliders, a braced door on the south face), an interior rung ladder (`COL_LADDER_CONSTRUCTION_CRANE_LADDER`, climbed from inside so it cannot be grabbed through the mast wall) leads 24 m up to a railed grating viewing ring, a ship ladder climbs on to the slewing deck, and the jib carries a grating catwalk with handrail lines out to the trolley plus the counter-jib deck — Props get a sky hide, everyone gets the map view. `harbor-warehouse.json` bounds.max.y 25 → 36 (client + server) so anti-cheat accepts positions on the crane. Zone GLBs may now ship explicit `COL_LADDER_<ZONE>_*` columns with a `ladderApproach` extra.
- Stop players walking through construction-site props: the AD zone GLB now ships namespaced `COL_MOVE_CONSTRUCTION_*` colliders for the crane mast and pad, material shelter posts and roof, cable reel, formwork stacks and rebar cages (validated against the procedural colliders for overlap, footprint and clearance), the shelter no longer pierces the frame column at (-42, -15), and the silo's leg bay is fenced with mesh skirts so its solid box collider reads as intended.
- Render first-person weapons and grenades after transparent world surfaces so pond water, windows, puddles, and roof skylights cannot blend over the viewmodel.
- AC residence routes (promoted `garden-ac.glb?v=20260908-ac02`): the chimney is now an open shaft — the pitched-roof colliders leave the 1.2 m flue free so a Hunter or Prop dropping into the chimney falls to the hearth and out into the living room (rim, soot lining, roof/ceiling/batten holes in the visuals; the bookshelf that stood in the flue moved to the front-right corner); the 2F loggia door is no longer blocked by the invisible cinematic house colliders (`COL_MOVE_CINE_HOUSE_FRONT_L/R/HEADER`, `HOUSE_LEFT`, `HOUSE_RIGHT` are dropped by the AC override, `HOUSE_BACK` stays); the four side window bands (1F/2F, both walls) lost their mullions and in-band shutters so players can jump out — shutters fold outside the opening. Procedural gameplay dump 488 → 492 (chimney roof split, bookshelf), `GardenHouseRoutes.test.ts` pins the flue, hearth exit, loggia door and window bands.
- Water is no longer an instant death line at the shore. The `waterHazards` boxes of `harbor-warehouse.json` (client + server) now end at the sea surface (`max.y` -0.9 instead of 25), so standing on a seawall cap, pier or fence over the water never counts; a Hunter or Prop who walks off the edge really falls in (`WaterVolumes` in `client/src/game/controllers/WaterSwim.ts`: over the sea the y=0 ground plane gives way to the swim level, feet -1.6, splash + shake on entry, swim speed × 0.55) and can climb back out by swimming into the shore (the shore lifts the body onto the land or the seawall cap, also when a wall blocks the body). The server (`DrowningTracker`, `WATER_DROWN_GRACE_MS` = 3000 in `shared/constants.ts`) drowns them only after three continuous seconds under water — also when inputs stop — leaving the water at any moment resets the clock; the HUD shows the countdown ("IN THE WATER — CLIMB OUT! 2.4 s") and the old drowning cinematic still plays on death. Tests: `WaterSwim.test.ts` (fall / swim / climb out for both controllers), server `EnvironmentalHazards.test.ts` (surface rule, grace clock, expiry sweep).
- BD shop gameplay boxes re-authored for the Harbor Market floorplan (`buildDocksideMiniMart`): 3 m door (43.5..46.5, glazing solid to the slab on both sides — the old 2 m visual door left walk-through glass between x 42..44 / 46..48), interior fixtures replaced by the produce display, two gondolas, fridge runs, checkout/impulse/wall shelf and the stockroom/manager partition walls (to the roof, doors open), and the west-side crate stair + ramp to the roof removed so the left side of the shop is a flat service path with no invisible boxes. Roof access moved inside instead (`?v=20260908-bd03`): a fixed ladder on the stockroom's west wall (`COL_LADDER_CONTAINER_BD_STOCK_LADDER`, approach from the room) climbs through the ceiling and a 1.1 × 1.2 m roof hatch (curb, yellow band, open lid, hoop) and the climber mantles out onto the west roof overhang; the procedural roof collider is split into four boxes around the hatch. Dump 492 → 490 → 493; all four zone contracts updated.

## [1.1.0] — 2026-07-14 · UI/Game Feel + Chế độ Lây Nhiễm + Map Trường Học

Spec: `docs/superpowers/specs/2026-07-14-infection-mode-school-map-design.md`

### Chế độ chơi mới: Infection (Lây nhiễm) 🧟
- Host chọn mode trong Game Settings (Classic ⇄ Infection); badge mode hiển thị cho cả phòng
- Prop bị hạ **không chết** mà biến thành Hunter ngay tại chỗ (hồi máu, đầy đạn) và đi săn đồng đội cũ
- Splash "BẠN ĐÃ BỊ NHIỄM!" + killfeed 🧟 riêng cho lây nhiễm
- Prop sống sót đến hết giờ nhận thưởng 250 điểm (thay 150 classic)
- Logic tách vào `PropDownHandler` + **7 unit tests** (conversion, win conditions, classic không đổi)

### Map mới: Trường Học (Sunny School) 🏫
- Tòa nhà 2 tầng: 4 phòng học (bàn ghế, bảng, tủ khóa), hành lang, cầu thang 2 đầu, tầng 2 có kệ sách
- Phòng gym trần cao (khán đài, nệm, bóng), căng tin (quầy, bàn tròn, tủ lạnh), sân trường (cột cờ, ghế đá, bồn cây, nhà xe)
- Cổng trường = gate spawn hunter (cùng cơ chế cổng harbor)
- 14 props mới để ẩn thân (tủ khóa, cặp sách, hộp sữa, bình cứu hỏa, quả địa cầu…)
- Host chọn map trong Game Settings; minimap tự đổi layout theo map
- Server: **map registry** (`getMapData`) — HitValidation/AntiCheat/SpawnManager/PropTransformValidator đều theo `config.mapId`, hỗ trợ đổi map giữa các trận (client tự teardown + rebuild map)

### Fix kèm theo
- ROOM_STATE gửi kèm `config` (mode/map/players/rounds) để lobby sync cho người không phải host
- Solo explore dùng đúng spawn của map đang chọn (trước bị kẹt spawn harbor)
- Prop vừa bị lây không còn nhận nhầm thông báo "STUNNED" từ lựu đạn

### UI/UX + Game Feel Makeover

Spec: `docs/superpowers/specs/2026-07-14-ui-gamefeel-makeover-design.md` · Commit: `317ea24`

#### Design system
- CSS design tokens (màu, spacing, radius, shadow, duration/easing) trong `styles.css`
- Font mới: **Bungee** (display) + **Be Vietnam Pro** (body) — hỗ trợ tiếng Việt đầy đủ

#### Hiệu năng & đồ họa
- `QualityManager`: 3 tier HIGH/MEDIUM/LOW tự phát hiện theo thiết bị (mobile → LOW), chỉnh tay được trong Settings, lưu localStorage
- Post-processing **UnrealBloom** (chỉ tier HIGH) qua `EffectComposer`

#### Game feel trong trận
- Hit marker ✕ trên crosshair (đỏ khi kill) + damage number bay lên tại vị trí mục tiêu
- Camera shake kiểu trauma: khi bắn, dính đạn, lựu đạn nổ (mạnh theo khoảng cách)
- FOV kick khi speed boost; khói "poof" khi biến hình

#### HUD
- Splash đầu round "BẠN LÀ THỢ SĂN / ĐỒ VẬT" + mô tả nhiệm vụ
- Banner chuyển phase ("CUỘC SĂN BẮT ĐẦU!"), số đếm ngược khổng lồ 5→1 + tick
- Timer nhấp nháy đỏ khi <30s; killfeed 💀 slide-in; ability chips với vòng cooldown conic-gradient + badge số lượng

#### Màn hình
- **Main menu**: nền 3D props xoay chậm, logo glow, layout card, phần tạo phòng/nhập mã thu gọn, room list tự refresh 5s, nút ⚙ Settings
- **Lobby**: avatar màu theo tên, click-copy mã phòng ("✓ Đã copy!"), chấm ready nhấp nháy
- **Results**: podium top-3 🥇🥈🥉 + confetti + hàng điểm trượt vào, tiêu đề song ngữ
- **Loading overlay**: spinner + tip gameplay + cảnh báo cold-start server Render

#### Âm thanh UI
- Hover/click/success/error/tick/stinger tổng hợp bằng WebAudio (không cần file), toggle trong Settings

#### Fix kèm theo
- Copy mã phòng có fallback `execCommand` + không bị state-sync 20Hz ghi đè feedback
- Kết quả round/match localize theo ngôn ngữ đang chọn

## [1.0.0] — 2026-03-19 · First Public Release

Game Prop Hunt multiplayer: ThreeJS client + Colyseus server, map Old Harbor, vai Hunter/Prop với abilities (grenade/scanner/boost/phase-walk · invisible/transform/speed/duplicate/soul), minimap, voice chat, sound memes, chat, scoreboard, mobile touch, song ngữ EN/VI, deploy Vercel + Render.

---

### Roadmap các phase tiếp theo
- **Phase 2 — Gameplay mới**: power-ups, taunt system, map mới, chế độ chơi, progression/rank
- **Phase 3 — Onboarding & retention**: tutorial người mới, thống kê cá nhân, lịch sử trận, flow vào game nhanh hơn
