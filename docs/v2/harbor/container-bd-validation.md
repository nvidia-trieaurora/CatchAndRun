# Harbor V2 — BD Eastern Container Yard validation (2026-09-07/08)

Status: validated as a staging candidate on 2026-09-07, then **promoted on request
(2026-09-08 00:50, "apply")**: the byte-identical candidate now lives at
`client/public/assets/maps/harbor-v2/zones/container-bd.glb` (sha256 `975bc659…b714c`,
2,025,396 B) with its `.metrics.json`, and `promoted: true` + `?v=20260908-bd01` in
`client/src/game/world/zones/harborZones.ts` makes production clients load it.
**Nothing was committed or pushed.** Version gate: package version 1.1.0, work recorded
under `CHANGELOG.md` → `[Unreleased]` (v1.2.0). Baseline for this pass: origin/main
`708d368`. Source of truth `art-source/harbor-v2/container-bd/container-bd.blend`,
generated by `tools/harbor-v2/zones/build_container_bd.py` (README next to the .blend);
the candidate stays in `_staging/` for `?harborZones=staging` reviews of later iterations.

## Audit findings that shaped the layout

- Legacy yard = cinematic zone `container-yard` (17 merged meshes, x 24..58.8, z -29.4..0.6)
  plus 14 `COL_MOVE_CINE_CONTAINER_*`, `COL_MOVE_CINE_FORKLIFT` (48.1..49.9, z -5.75..-4.25)
  and `COL_MOVE_CINE_YARD_OFFICE`. The Mini Mart ("NEON MART") lives in the cinematic
  `dock` meshes shared with the Dockside Bar (`dock_MESH_concrete_warm/roof/glass_warm/
  container_teal/neon_cyan`) with 8 `COL_MOVE_CINE_MART_*` colliders: door x 44..46 at
  z -33, 2.8 m clear. Procedural colliders in the box: mart slab/roof/canopy/interior,
  vending machines, roof-access crates + ramp, bins/boxes/pallet in front of the shop,
  the tire stack (41.5..44.2 × -10.5..-9.5), lamp posts (50,-28) (55,-3), seawalls.
- Legacy containers 10–12 stood 3.7 m from the storefront; container 05 enclosed the prop
  spawn (40,-20); the cinematic forklift collider sat where the new lane runs; several
  procedural colliders (vending machines, shelves, ramp) had no visual in production.
- Ground: the island asphalt is one slab with its top at y 0.18 while players walk at
  y 0; paint sits at 0.19..0.21, the Mini Mart slab at 0.25 — the new forecourt sidewalk
  matches that slab.

## Candidate contents and budgets

| Metric | Value | Budget |
| --- | --- | --- |
| LOD0 | 14 meshes / 22,912 tris / 14 draws | ≤50,000 / ≤18 |
| LOD1 | 6 meshes / 346 tris / 6 draws | ≤12,000 / ≤10 |
| Payload | 2,025,396 B (1.93 MiB) | ≤6 MiB |
| Textures | 17 WebP images, 160 KB on disk, 14.5 MiB estimated GPU | ≤16 MiB added, ≤1024 px |
| Zone colliders | 47 | ≤60 |
| Procedural parity | 488 | 488 |

`validate_zone.py` → `passed: true`, 0 warnings; `gltf-transform validate` → 0 errors,
0 warnings, 0 hints. Materials: one vertex-tinted corrugated sheet set (COLOR_0 tints
for teal / brick red / navy / faded ochre / reefer cream / workshop grey / cladding,
grime gradient at the base, dulled roofs), `steel_dark`, `concrete_yard`, grating +
grass alpha cards, dark shop glass (depthWrite handled by `MapAssetLoader`), puddles,
flat palette, emissive light palette.

## Layout gates from the brief

| Requirement | Result |
| --- | --- |
| Main forklift lane ≈6 m, N–S, markings + drains, no clutter | x 44.2..50.2, z -25..2.5; validator clearance volume `main-lane` (0.25..3.5 m) is empty |
| ≥2 secondary alleys ≈3 m, no unreasonable dead ends | A z -16.51..-13.45 (3.06 m), B z -11.01..-7.7 (3.31 m); both run from the west margin to the lane |
| Two-high blocks west + south, single stack on the waterfront, no random mid-yard stacks | W1/W2/W3+20 ft, S1 two-high; E1/E2 single along the seawall; loading zone and machinery only east |
| Turning bay + loading zone | hatched turning bay z -25..-19 east of the lane; painted loading zone 51.1..58 × -12.4..-4.8 with trailer, forklift, barriers, cones |
| Forecourt ≥6 m, nothing before door/glass, entrance visible from the lane, sidewalk + kerb | 6.5 m raised sidewalk z -32.5..-26 at slab level, kerb, bollards only at the kerb line; `shop-entrance` + `shop-doorway` clearance volumes empty; the lane points at the door |
| Elevated route only on valid colliders | mart roof reached by the legacy crate/ramp colliders (now a service stair); roof parapets get 0.56 m colliders with a gap at the landing; no new platforms |
| Spawns untouched and clear | (35,-15) alley A, (38,-10) alley B, (40,-20) and (30,-25) cross road — all four are clearance volumes |

## Collision

- Zone colliders ship with the visuals (`COL_MOVE_CONTAINER_BD_*`): 21 containers (exact
  2.438 × 2.591 envelopes incl. the trailer box), trailer, forklift, handler, 6 forecourt
  sidewalk pieces (0..0.25, split around the legacy bin/box/lamp colliders), 6 bollards,
  bench, 5 roof parapets, south fence, 2 gate posts, 2 pallet stacks, 3 reels, drums, cage,
  3 barriers, tarp stack. Validator: none overlaps a procedural collider (> 0.002 m³), none
  leaves the footprint, none enters a clearance volume.
- Removed legacy colliders (runtime `removeNames`, mirrored in the contract):
  `COL_MOVE_CINE_CONTAINER_00..13`, `COL_MOVE_CINE_FORKLIFT`. Kept: all `MART_*`,
  `YARD_OFFICE`, procedural mart interior/exterior, tire stack, lamps, seawalls.
  Runtime `colliderCount` 703 → 735. `ContainerBDZone.test.ts` proves the override drops
  exactly those names and keeps the mart header (doorway lintel) and office, and checks the
  validated metrics: no zone collider in the doorway approach, in the main lane or on a
  spawn.
- No collider on paint, puddles, weeds, fence mesh, gate leaves, cables, cargo net, cones,
  wheel stops, loose pallets, bike, hand cart, small crates.
- `harbor-warehouse.json` untouched (no map-parity change).

## Runtime (headless Chrome 1600×900, solo explore, high tier, `?harborZones=staging`)

| Configuration | Backend | Draw calls | Triangles | GPU textures | Est. texture memory | Colliders | FPS avg | Frame p95 | Console |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Production (AC + AD + Ferris) | WebGPU | 217 | 194,762 | 229 | 119.9 MiB | 703 | 60 (vsync) | 17.5 ms | 0 errors |
| + BD (staging) | WebGPU | 221 (+4) | 190,224 (−4.5k) | 247 (+18) | 130.6 MiB (+10.7) | 735 | 60 (vsync) | 17.3–17.5 ms | 0 errors |
| + BD (staging) | WebGL2 | 217 | 179,976 | 234 | 130.6 MiB | 735 | 60 (vsync) | 17.4 ms | 0 errors |

The zone replaces the 17 legacy yard meshes and adds 14 batches (net +4 draws, fewer
triangles). One WebGPU capture that overlapped a Blender Eevee render read 47 FPS /
30.7 ms p95 (GPU contention); the clean re-run is 60 FPS / 17.3–17.5 ms, identical to the
baseline. Scene-wide texture memory is now ≈130 MiB (desktop gate 128 MiB from the
earlier passes): the zone's own +10.7 MiB is under its 16 MiB budget, but the next
promotion round should convert the AC/AD/warehouse sets to KTX2 or 512 px variants.

Snapshots (`art-source/harbor-v2/_staging/snapshots/`, disposable): `before-bd-webgpu-*`,
`after-bd-webgpu-*`, `after-bd-webgl2-*`, `after-bd2/3/4-webgpu-*` (preset tuning) for
`overview`, `containerBD`, `containerBDRoute`, `containerBDShop`, `containerBDReverse`,
`containerBDTop`, `containerBDDetail`, `containerBDForklift`, `containerBDRoof`; Blender
Eevee previews `_staging/renders/bd-blender-*.png`.

Fixes made from captures: main lane shifted 0.5 m east so the legacy tire-stack collider
stays out of it; container tints brightened (navy read black in shadow) and grime lifted
0.5 → 0.68; the concrete base band split around the door (it crossed the opening); door
leaves folded onto the jambs (they cut through the 2 m opening); end mullions/sills kept
inside the window spans; corner castings flush with the envelope; rope coils / life ring
moved off the seawall walkway; presets re-aimed.

## Teardown (six create → explore → leave cycles, `_staging/teardown_cycles.mjs`)

geometries 287, 341, 395, 449, 503, 557; textures 247, 249, …, 257; draw calls 222 constant;
0 console errors. The +54 geometries / +2 textures per cycle is the pre-existing base
session pattern measured with `?harborZones=off` and with production zones (see
`ferris-harbor-district-validation.md`); the BD zone adds no per-cycle growth.

## Test / lint / build (`_staging/verify-bd.log`, 2026-09-07 23:10)

- `tsc -p client --noEmit`: clean.
- `npm run test -w client -- --run`: 19 files, **99 passed** (95 before). New
  `ContainerBDZone.test.ts` (4): registry gate + contract mirror of the removed colliders;
  override drops exactly the 15 legacy colliders and keeps the mart header / office while
  removing legacy yard meshes and in-box clutter only; multi-zone carving of one shared
  merged batch; zone collider ingestion + validated-metrics checks (doorway approach, main
  lane, spawns, budgets). `FirstPersonViewmodel.test.ts` (glass vs weapon) still green.
- `npm run lint`: 0 errors (1,101 pre-existing warnings repo-wide; 3 pre-existing style
  warnings in `MapAssetLoader.ts`).
- `npm run build` (shared → server → client): success.
- Server `vitest run`: 85 passed / 10 failed — the **same 10 baseline failures** that also
  fail at the committed v1.1.0 HEAD (GameplayFlow ×3, MatchStateMachine ×1, RoleAssigner
  ×2, ScoringSystem ×4); no gameplay code touched here.
- Meshopt: the candidate is written by the zone exporter without Meshopt compression
  (same as AC/AD/Ferris — compression is applied to the base cinematic GLB only); texture
  compression = WebP (`EXT_texture_webp`), validated by `gltf-transform validate`.

## Files changed in this pass (none committed)

New: `tools/harbor-v2/zones/build_container_bd.py`, `tools/harbor-v2/zones/contracts/
container_bd.json`, `art-source/harbor-v2/container-bd/{container-bd.blend,README.md}`,
`tools/harbor-v2/textures/container-bd-pbr/_derived/*` (3 derived sets + palettes),
`client/tests/ContainerBDZone.test.ts`, this document, `_staging/container-bd-candidate.glb`
+ `.metrics.json` (gitignored).
Modified: `tools/harbor-v2/zones/zone_kit.py` (`pbr(vertex_tint=)`, `TintedMaterial`),
`client/src/game/world/assets/MapAssetLoader.ts` (carve every zone's `carveNames`),
`client/src/game/world/zones/harborZones.ts` (BD entry, `CONTAINER_BD_REMOVED_COLLIDERS`),
`client/src/game/GameManager.ts` (8 dev presets), `package.json` (harbor:v2:*-container-bd
scripts), `CHANGELOG.md`.

## Promotion (done 2026-09-08 00:50)

Procedure from `edge-zones-ac-ad-validation.md`: candidate + metrics copied to
`client/public/assets/maps/harbor-v2/zones/container-bd.glb` (sha256 verified equal),
`promoted: true` and `?v=20260908-bd01` set, `ContainerBDZone.test.ts` now asserts the
promoted state and the cache token, `harbor:v2:test-container-bd` (21 tests, includes the
"production GLB exists for every promoted zone" check) and `tsc` re-run clean, then the
production URL (no `?harborZones`) captured on WebGPU and forced WebGL2 together with the
AC / AD / Ferris presets — `_staging/snapshots/promoted-bd-webgpu-*` /
`promoted-bd-webgl2-*`, `_staging/verify-bd-promoted.log` (lint / build / full client
suite). Not committed.

## Harbor Market redesign (2026-09-08 — validated as a candidate, then PROMOTED on request as `?v=20260908-bd02`)

The shop was rebuilt as the HARBOR MARKET mini-supermarket by
`tools/harbor-v2/zones/harbor_market.py` (called from `build_container_bd.py`; the
yard is unchanged). The candidate `_staging/container-bd-candidate.glb` (3.94 MB) was
verified as below and, on Thinh's "apply", copied byte-identical (sha256 1a8c4114…) to
`client/public/assets/maps/harbor-v2/zones/container-bd.glb` (+ metrics), token
`?v=20260908-bd02`; `harbor:v2:test-container-bd` (21 tests) and a production WebGPU
capture (`promoted-hm-*.png`, 0 console errors) re-run after the copy. Not committed.

Audit: shop source = `build_shop` in `build_container_bd.py` (Mini Mart on the legacy
colliders, roof service stair + crates on the west); gameplay boxes =
`buildDocksideMiniMart` in `oldHarborFortnite.ts`; carve box `CONTAINER_BD_MART_BOX`;
door 44..46 with walk-through glass at 42..44 / 46..48 (legacy front colliders end at
42 / 48 while the visual door was 2 m). References copied to
`_staging/reference/harbor-market/` (9 images) and read for layout; the top floorplan set
the interior, the detail sheet the fixture kit, the exterior/front views the proportions.
Baseline snapshots `_staging/renders/harbor-market/before-hm-*.png` (production, WebGPU).

Gameplay boxes (procedural, `buildDocksideMiniMart`, dump 492 → 490, all four contracts
updated): front glazing 38..43.5 / 46.5..52 solid to the slab, header 43.5..46.5 × 4.0..5.5
(3 m automatic door, 2.8 m clear kept); interior fixtures each one box — produce display
38.2..39.5 × -39.6..-34.6 (+ crate stack -34.5..-33.6), gondola A 41.2..42.2 × -38.95..-35.15,
gondola B 45.6..46.6 × -40.35..-35.15, rear fridges 43.2..51.05 × -42.8..-42.05, right
fridges 51.05..51.8 × -42.05..-38.05, checkout 48.7..49.7 × -37.6..-34.4, impulse rack
48.2..48.7 × -37.6..-36.4, cashier wall shelf 51.4..51.8 × -37.4..-35.2, stockroom /
manager room 38.2..43.0 × -42.8..-40.5 (walls to the roof, doors 39.7..40.6 and
41.9..42.8, stock shelving, desk); removed — legacy shelves, counter, fridge, arcade
cabinets, the three west crates and the crate stair + six ramp steps (the west side is now
flat and collider-free; the shop roof is no longer reachable). Aisles: produce 1.7 m,
centre 3.4 m (door axis), checkout lane 1.6 m, front 2.0 m, rear 1.7 m. Zone colliders:
45 (parapet now closed on the west, `CRATES_E` for the service-side stack, four bollards,
bench, forecourt slabs, containers/machinery as before). Contract clearance volumes added:
`shop-left-side`, `shop-entrance-in`, `shop-aisle-front/centre/produce/rear`,
`shop-checkout-lane`, `shop-room-door-a/b`; `shop-doorway` widened to 43.5..46.5; budgets
raised for the interior (LOD0 62k tris / 20 draws, LOD1 16k / 11, payload 8 MiB, GPU
textures 28 MiB).

Candidate contents: charcoal vertical cladding (vertex-tinted container sheet), warm
concrete plinth, membrane roof + parapet + flashing + gutter + downspouts, the roof
continuing 2 m over the entrance on the legacy canopy collider with a deep fascia carrying
the sign — real text geometry "HARBOR MARKET" (DIN Alternate Bold, converted to mesh,
1344 tris, 5.2 m wide, warm-white emissive on a navy panel), storefront glazing
39.6..43.5 / 46.5..50.4 with mullions and transom, bi-parting door leaves parked open with
operator header + cyan LED, 0.7 m cyan canopy strip, east service side (recessed door,
lamp, stacked condenser + electrical cabinet on the legacy 0.9 × 0.8 boxes, conduit,
meter box, crate stack, pallet, hand truck), west painted walkway with three drain
grates and two wall lamps at 3.5 m. Interior: floor with joints, cream lining with dark
dado, suspended ceiling with 3 × 4 LED panels (+2 room lights), entrance mat, two
anti-theft pedestals (visual only, outside the doorway volume), three baskets, produce
display (17 crates, fruit heaps + loose pieces, chalkboard), two gondolas (4 shelves per
side, price lips, endcaps), six reach-in fridges (glass doors, handles, lit back panel,
wire shelves, product row cards), checkout (belt, scanner well, register, monitor,
terminal, bagging shelf, bag rail), impulse rack, cashier wall shelf, stockroom shelving
with cartons, manager desk/monitor/chair, office window, fire extinguisher + sign, rear
conduit. Products: 917 objects from one procedural 512 px label atlas (16 cells: cans,
boxes, jar, bottles, carton, bag, three row cards), vertex-tinted in five variants, UV
locked — `MAT_PRODUCT_ATLAS` is one draw call. Shop share: LOD0 24,208 tris across 8
materials (only the atlas and `MAT_GLASS_CLEAR` are new draws), LOD1 346 tris / 6
materials; zone LOD0 16 draws / 46,172 tris, LOD1 7 / 630; GPU texture estimate within
the 28 MiB budget; validator `passed: true`, glTF validator clean.

Runtime (`?harborZones=staging`, WebGPU, headless Chrome 1600×900): 9 presets captured
(`after-hm-*.png`: yard overview, exterior, front, left side — flat, no stair —
interior entrance, checkout reverse, top, shelf close-up, fridge close-up), 0 console
errors; forced WebGL2 `after-hm-webgl2-*.png`, 0 console errors. Teardown
(`teardown-cycles-report.json`, 6 cycles): 59.6–59.7 FPS, p95 17.4–17.5 ms, 218 draw
calls, 194,870 triangles, 704 colliders, texture memory ≈130 MiB, geometries +54 / textures
+2 per cycle — the pre-existing base-session leak, unchanged from the promoted BD run.

Tests / lint / build (`_staging/verify-hm.log`): `tsc` clean, client 21 files / 107 tests
pass (new `HarborMarket.test.ts`: left side flat, 3 m door with solid glazing, one box per
fixture, aisles ≥ 1.5 m, rooms enterable, zone colliders outside approach/aisles, shop
≤ 35k tris / 15 materials via `_staging/container-bd-build-stats.json`), server map
parity pass, lint 0 errors, production build ok. Blender previews `blender-hm-*.png`
(exterior, front, left, interior, checkout, floorplan cut-away at y 4.1, shelf close-up).

Promoted 2026-09-08 (`?v=20260908-bd02`), then re-promoted the same evening as
`?v=20260908-bd03` (sha256 77920f19…, 4.0 MB) with the roof access below.

### Roof access from inside (bd03)

Thinh agreed to keep roof play after the west stair went: a fixed ladder on the
stockroom's west wall (`COL_LADDER_CONTAINER_BD_STOCK_LADDER`, 38.25..38.55 ×
0.25..5.8 × -41.55..-41.05, approach `+x`) climbs through the suspended ceiling and a
1.1 × 1.2 m roof hatch (38.2..39.3 × -41.9..-40.7: steel curb with yellow band, lid
standing open, shaft lining, stile hoop) — the climber mantles out onto the west roof
overhang. The procedural roof collider is split into four boxes around the hatch
(dump 490 → 493, all contracts updated; `HarborMarket.test.ts` asserts the hole is
open, the roof around it solid and the ladder shipped exactly where the contract says;
clearance `shop-roof-exit` keeps the landing spot clear). Validator / glTF / tests /
lint / build re-run, production captures `promoted-hm-bd03-*` (WebGPU + WebGL2, 0
console errors). Presets `harborMarketRoofLadder`, `harborMarketHatch`. Note the hatch
is also a drop from the roof into the stockroom.

## Not done / follow-ups

- Harbor Market: the automatic door is parked open (no interaction system, no moving
  collider).
- Headed FPS / p95 on the target GPU (headless Chrome is vsync-capped at 60 FPS).
- Scene-wide texture memory ≈130 MiB with all four zones resident: convert the older zone
  sets to KTX2 or 512 px variants before the next promotion.
- Forklift beacon uses `lamp-flicker` (no runtime rotation system); dust particles not added.
- The office spawn (28, 3.3, 2) and the legacy north-south road west of x 29.5 are outside
  the footprint and untouched.
