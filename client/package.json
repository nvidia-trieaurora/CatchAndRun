{
  "name": "@catch-and-run/client",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ../tools/scan-memes.js && vite",
    "build": "node ../tools/scan-memes.js && tsc -p tsconfig.build.json && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@catch-and-run/shared": "*",
    "colyseus.js": "^0.15.0",
    "three": "0.181.0"
  },
  "devDependencies": {
    "@types/three": "0.181.0",
    "vite": "^6.0.0"
  }
}
